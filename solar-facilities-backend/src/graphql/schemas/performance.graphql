type Performance {
  id: ID!
  activePower: Float!
  energy: Float!
  timestamp: String!
}

input ReadPerformanceInput {
  facilityId: ID!
  startDate: String!
  endDate: String!
}

type Query {
  performanceData(input: ReadPerformanceInput!): [Performance]
}
